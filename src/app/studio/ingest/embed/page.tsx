import { ComingSoon } from "@/components/ComingSoon";

export default function Page() {
  return <ComingSoon chip="Studio" title="Paste URL" body="Wiring up next — pending storage backend + auth." />;
}
