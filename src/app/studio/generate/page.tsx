import { ComingSoon } from "@/components/ComingSoon";

export default function Page() {
  return <ComingSoon chip="Studio" title="Generate" body="Wiring up next — pending storage backend + auth." />;
}
