import { ComingSoon } from "@/components/ComingSoon";

export default function Settings() {
  return (
    <ComingSoon
      chip="Settings"
      title="Settings"
      body="Workspace · billing · API keys — coming with auth."
      size="lg"
    />
  );
}
